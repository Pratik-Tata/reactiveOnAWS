spring.application.name=demo

# Keep logs chatty for study purposes so you can follow each request.
logging.level.com.reactive.demo=DEBUG
logging.level.org.springframework.web=INFO
logging.level.reactor.netty.http.server=DEBUG

# Netty runs the reactive HTTP server for WebFlux by default.
server.port=8080

app.message=default-message